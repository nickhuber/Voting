<h1>Listing answers</h1>

<table>
  <tr>
    <th>Body</th>
    <th>Question</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @answers.each do |answer| %>
  <tr>
    <td><%= answer.body %></td>
    <td><%= answer.question %></td>
    <td><%= link_to 'Show', question_answer_path(answer) %></td>
    <td><%= link_to 'Edit', edit_question_answer_path(answer) %></td>
    <td><%= link_to 'Destroy', question_answer_path(answer), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Answer', new_question_answer_path %>
