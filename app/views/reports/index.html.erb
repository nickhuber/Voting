<% content_for :title do %>
  Listing Reports
<% end %>

<% content_for :notice do %>
  <%= notice %>
<% end %>

<!-- TODO: This link probably shouldn't exist -->
<div class="button-create">
<%= link_to 'New Report', new_report_path, :remote => true %>
</div>
<!---->

<table class="listing-table results">
  <tr>
    <th>Poll Name</th>
    <th>Polled</th>
    <th>Number of questions</th>
    <th>Number of Participants</th>
    <th></th>
    <th></th>
  </tr>

  <% @reports.each do |report| %>
  <tr>
    <td><%= report.poll.title %></td>
    <td><%= time_ago_in_words(report.created_at) %> ago</td>
    <td><%= report.poll.questions.count %></td>
    <td><%= report.participants.count %></td>
   
    <td class="tblcol-link"><%= link_to 'Show', report %></td>
    <!-- <td class="tblcol-link"><%= link_to 'Edit', edit_report_path(report) %></td> -->
   
  </tr>
  <% end %>
</table>
