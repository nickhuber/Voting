var question = $(".question-<%= @question.id %>").remove();
var question_link = $(".question-link", question);
var link = "<%= escape_javascript(link_to 'Add', add_to_poll_path(@poll.id, @question.id), :remote => true) %>";

$("a", question_link).remove();
$(question_link).append(link);

$("#all-questions tbody").append(question);